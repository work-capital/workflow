defmodule Workflow.Router do
  @moduledoc"""
  Listen to all events, and dispatch them based on correlation id's, so process managers are automatically
  subscribed (push) and will start or continue based on this events. 
  """



end
